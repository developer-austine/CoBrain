import SettingsClient from "./_components/SettingsClient";

/** Account settings: profile picture, display name, and the danger zone. */
export default function SettingsPage() {
  return <SettingsClient />;
}
